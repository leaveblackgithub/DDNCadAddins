{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch .NET Framework App",
      "type": "coreclr",
      "request": "launch",
      "preLaunchTask": "Build .NET Framework 4.7",
      "program": "${workspaceFolder}/bin/Debug/x64/YourProject.exe",
      "cwd": "${workspaceFolder}"
    }
  ]
}