---
description: 
globs: 
alwaysApply: true
---

# 使用CAD .NET 2019 API
# 捕捉所有异常，用OperationResult返回调用函数，不允许抛出任何异常。
# 除了命令模块和输入输出模块，其他模块不应该包含输入输出，应该把输入当成参数，输出当成返回结果，由命令模块来提供输入，和按需输出
# 把所有与CAD API的交互都封装起来，确保每个访问都使用同一个入口